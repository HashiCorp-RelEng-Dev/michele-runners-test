name: lint

on:
  push:

jobs:
  lint:
    runs-on: [custom, ubuntu, small]
    steps:
      -
        name: Checkout
        uses: actions/checkout@v3
      -
        name: Run linter
        run: golangci-lint run main.go